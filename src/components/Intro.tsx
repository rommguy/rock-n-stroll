import React, { FunctionComponent } from 'react'
import { RouteProps } from '../types'

export const Intro: FunctionComponent<RouteProps> = () => {
    return <div className="intro-root">I am intro</div>
}
